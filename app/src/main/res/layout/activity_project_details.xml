<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ui.addProject.AddProjectActivity"
    tools:menu="@menu/add_project">

    <include layout="@layout/toolbar" />

    <include layout="@layout/content_project_details" />

    <ru.dimorinny.floatingtextbutton.FloatingTextButton
        android:id="@+id/floatin_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_margin="@dimen/fab_margin"
        app:floating_background_color="@color/colorPrimary"
        app:floating_left_icon="@drawable/ic_save_24dp"
        app:floating_title="Add to projects"
        app:floating_title_color="@android:color/white"
        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        style="@style/MyFabStyle"
        app:srcCompat="@drawable/ic_add_24dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>